#ifndef DLDL_GENERATE_TOOL_PROJECT_H
#define DLDL_GENERATE_TOOL_PROJECT_H

#include <filesystem>
#include <fstream>

namespace DLDL::generate::tool
{
	/*!	\fn Project
	 *	\brief Generates the Tool project, this consists of various directories.
	 *	\details The set of directories are not bound to some directory,
	 *	the user is required to encapsulate the directories to the right paths.
	 */
	class Project
	{
	};
}

#endif // DLDL_GENERATE_TOOL_PROJECT_H
